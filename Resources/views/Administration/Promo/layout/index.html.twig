{% extends '::layout/layoutAdmin.html.twig' %}
{% block stylesheets %}
        <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
        <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap-theme.min.css">       
{% endblock %}
{% block body -%}    
    <div class="col-lg-6">
       <div class="panel panel-primary">
           <div class="panel-heading">
               <h2>Liste des code promo</h2>
           </div>
           <div class="panel-body">
               <div class="row-fluid">
                   <fieldset>
                       <legend>Tableau des codes promo</legend>     

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Pourcentage réduction</th>                
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('adminPromo_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.id }}</td>
                <td>{{ entity.nomReduction }}</td>
                <td>{{ entity.pourcentage }}</td>                
                <td>
                    <a href="{{ path('adminPromo_show', { 'id': entity.id }) }}"><i class="icon-book"></i></a>
                    <a href="{{ path('adminPromo_edit', { 'id': entity.id }) }}"><i class="icon-edit"></i></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
         <a type="button" class="btn btn-primary" href="{{ path('adminPromo_new') }}">
                <i class="icon-plus"></i>
            </a>     
    </table>
    </fieldset>
    </div>            
   </div>
</div>
            </div>
              
    
    {% endblock %}
